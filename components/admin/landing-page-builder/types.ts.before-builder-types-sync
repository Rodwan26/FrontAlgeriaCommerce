export type PreviewMode = "desktop" | "mobile";

export type SectionType =
  | "hero"
  | "features"
  | "product"
  | "testimonials"
  | "order";

export type LandingSection = {
  id: string;
  type: SectionType;
  title: string;
  enabled: boolean;
};

export type LandingSettings = {
  primaryColor: string;
  secondaryColor: string;
  backgroundColor: string;
  textColor: string;
  buttonText: string;
  fontFamily: string;
};

export type SectionMoveDirection = "up" | "down";

export type SidebarResizeLimits = {
  min: number;
  max: number;
  collapsed: number;
};
